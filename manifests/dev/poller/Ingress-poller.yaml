apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: poller
  namespace: demo
spec:
  rules:
    - host: poller.universe.home
      http:
        paths:
          - backend:
              service:
                name: poller
                port:
                  number: 3000
            path: /
            pathType: Prefix
