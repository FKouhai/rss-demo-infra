apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: frontend
  namespace: demo
spec:
  rules:
    - host: rss.universe.home
      http:
        paths:
          - backend:
              service:
                name: frontend
                port:
                  number: 4321
            path: /
            pathType: Prefix
